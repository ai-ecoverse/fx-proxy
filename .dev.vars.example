# Copy to .dev.vars for `wrangler dev`. Never commit the real file.
AI_GATEWAY_API_KEY=
# PROXY_API_KEY=
# SEARCH_PROVIDER=perplexity   # uses the gateway key; no SEARCH_API_KEY
# SEARCH_API_KEY=              # only for brave | tavily | exa | serper
